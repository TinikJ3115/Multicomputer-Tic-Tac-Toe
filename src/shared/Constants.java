package shared;

public final class Constants {
    // Edit default network settings here, especially the default port and fallback host.
    public static final int DEFAULT_PORT = 5000;
    public static final String DEFAULT_HOST = "127.0.0.1";
    public static final String EMPTY_CELL_TOKEN = "_";

    private Constants() {
    }
}
